# auto-generated by grc.converter

id: iqlevels_iqlevels
label: IQ Level Dump
category: '[Utilities]/IQ Levels'

parameters:
-   id: sample_rate
    label: Sample Rate (Sps)
    dtype: real
    default: samp_rate
-   id: showlevels
    label: Show Peak IQ Levels
    dtype: enum
    options: ['1', '0']
    option_labels: ['On', 'Off']

inputs:
-   domain: stream
    dtype: complex

templates:
    imports: import iqlevels
    make: iqlevels.iqlevels(${sample_rate}, ${showlevels})

file_format: 1
